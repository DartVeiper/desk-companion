# Desk Companion — состояние работ

Порядок из п.8 плана плюс то, что добавилось по ходу. Отмечать здесь, а не
в памяти сессии.

## Где мы сейчас

**13.09.2026 — экран работает. Следующий узел — энкодер.**

Pi прошит и настроен, гребёнка припаяна, экран подключён и показывает
настоящий интерфейс. Драйвер ST7796S правок не потребовал. Подробности —
в «Журнале сборки» внизу.

Как подключать дальше — [СБОРКА.md](СБОРКА.md). Схема экрана картинкой:
[pi/preview/подключение-экрана.png](pi/preview/подключение-экрана.png).
Отмечать сделанное — здесь, в разделе «Железо, по порядку».

Ближайшее:

- [x] Железо закуплено: гребёнка PLD-40, microSD, кабель, зарядка
- [x] Карта прошита, ssh отвечает, код доставлен, система настроена
- [x] Гребёнка припаяна, замыканий нет (`tools/find_bridges.py`)
- [x] Экран подключён и работает — 9 проводов, `VCC` на 3,3 В (pin 17)
- [ ] **Разветвитель для шины SPI** — макетная плата, клеммник или Y-провода.
      Без него тач не подключить: три его сигнала идут на занятые 23, 19 и 21
- [ ] **Следующий узел — энкодер**: 5 проводов, ни с чем не пересекается,
      пока едет разветвитель
- [ ] **Сверить маркировку радара** — на плате должно быть `5V · GND · TX · RX · OUT`
      и `HLK-LD2410`. Если вместо TX/RX там DO/AO — это доплеровский модуль без UART,
      он не умеет статичное присутствие, и шаг 4 переписывается целиком

## Сделано (без железа)

- [x] Каркас рендера: `theme.py`, `state.py`, два бэкенда дисплея
- [x] Реестр экранов и `config.toml` — состав правится без кода (бэклог п.12)
- [x] Семь режимов по п.10 в едином стиле «карточки»
- [x] Три стиля покоя: ночной красный, крупные цифры, точечная матрица
- [x] Веб-превью с эмулятором ввода, ползунками и горячей перезагрузкой
- [x] Шрифт Nunito в репозитории — превью и устройство рисуют одним и тем же
- [x] Тап по карточкам → экраны подробностей (погода, воздух)
- [x] Ambient-режим по присутствию: пауза 3 мин, ротация стилей, ночью красный
- [x] Меню настроек по удержанию 3 с + полоса прогресса удержания
- [x] Строка состояния — молчит, пока всё в порядке; экран диагностики
- [x] Частичная перерисовка: полосы по вертикали + обрезка по горизонтали.
      Смена минуты — 16 КБ вместо 300, проверено на побайтном восстановлении кадра
- [x] Главный цикл `app/main.py` с источниками-заглушками: 7,5 мс на кадр,
      2,9% процессора, экономия по шине в 48 раз
- [x] Автосброс ручного статуса через 5 ч (шаг 8 плана)
- [x] Драйвер ST7796S — **проверен на живой плате 13.09.2026, правок не потребовал**

## Железо, по порядку

Каждый узел — подключить и сразу проверить своим шагом `bringup.py`.
Подробности и разбор типовых отказов — в [СБОРКА.md](СБОРКА.md).

- [ ] **Шаг 1** — прошить Pi, поднять Mosquitto и SQLite.
      Скрипты готовы: `pi/setup-step1.sh`, `pi/check-step1.sh`
      - [x] карта прошита, ssh отвечает
      - [x] вход по ключу вместо пароля — `deploy.sh` иначе спрашивал бы его пять раз
      - [x] `deploy.sh` довёз код на плату
      - [x] `setup-step1.sh` + перезагрузка; `check-step1.sh` — 13 из 13,
            `serial0 -> ttyAMA0`
      - [x] `setup-step2.sh` — библиотеки встали, все импорты проходят
      - [x] `bringup.py kernel` — 5 из 5, обе шины SPI, I2C и UART на месте
      - [x] гребёнка припаяна, проверена, плата после пайки грузится
- [x] **Шаг 2а** — драйвер ST7796S ожил на плате **без единой правки**:
      MADCTL, порядок цветов и 32 МГц подошли как были. `bringup.py display`
      2 из 2, приложение целиком отрисовало 53 кадра на живом экране
- [ ] **Шаг 2в** — тач XPT2046: CE1 (GPIO7, pin 26), шина не выше 2 МГц.
      IRQ **не нужен** — драйвер опрашивает панель по шине, T_IRQ остаётся
      не подключённым. Дальше `touch_calibrate.py`, потом пороги в `gestures.py`.
      Проверка: `bringup.py touch`
- [ ] **Шаг 3** — энкодер KY-040 с антидребезгом. Пороги удержания те же: 0,7 с и 3 с.
      Проверка: `bringup.py encoder`
- [ ] **Шаг 4** — LD2410 по UART. Питание 5 В, линии TX/RX перекрещиваются.
      Заложить отдельный вечер на калибровку порогов. Проверка: `bringup.py radar`
- [ ] **Шаг 5** — SCD41 по I2C + NTP. Отключить самокалибровку ASC (п.6).
      Проверка: `bringup.py i2c`, затем `bringup.py air`
- [ ] **Шаг 6** — ПК-агент на C#. Перед этим закрыть брокер паролем:
      сейчас `allow_anonymous true`, напоминание в конфиге mosquitto
- [ ] **Шаг 9** — LibreHardwareMonitor → Режим 5. Задача в Планировщике с наивысшими правами
- [ ] **Шаги 10–11** — накопить 1–2 недели `activity_minute`, потом IsolationForest на Pi
- [ ] **Шаг 12** — периодический бэкап SQLite + кнопка «скачать базу»
- [ ] **Шаг 13** — nowcast Яндекс.Погоды (ключ получить заранее) и стрик привычек
- [ ] **Шаг 14** — дашборд: сегодня, неделя, настройки. На том же FastAPI, что превью
- [ ] **Шаги 15–16** — тест-план п.9 целиком, потом корпус

## Добавилось по ходу

- [ ] Живая панель калибровки LD2410 — график энергии по 9 зонам.
      Без неё калибровка порогов из п.9 это гадание вслепую
- [ ] Панель здоровья блока в вебе — источник данных общий со строкой состояния
- [ ] Лента событий `state_events` таймлайном — отладка эвристик присутствия
- [ ] Ночная яркость: привязать палитру к PWM подсветки на GPIO18
- [ ] Настройки стилей в веб-дашборде — на устройстве только яркость и диагностика
- [ ] Фоновые картинки под ambient. Пиксель-арт при 144 PPI выигрывает у фото
- [ ] Вертикальный свайп → мини-меню быстрых действий. Жест свободен, и он
      разрешает конфликт, отмеченный в самом п.10 плана

## Журнал сборки

Сюда — одной строкой с датой всё, что вскрылось на живом железе и не
влезает в галочку: какую скорость шины пришлось поставить, где цвета
оказались перепутаны, какой провод на самом деле куда идёт. Галочки выше
говорят «сделано или нет», а этот журнал — «что мы узнали». Именно его
нужно читать первым, когда возвращаешься к проекту после перерыва.

- 12.09.2026 — железо закуплено целиком, начата физическая сборка
- 12.09.2026 — уточнено по коду: тачу не нужен IRQ, `T_IRQ` никуда не идёт
- 12.09.2026 — по памяти на 512 МБ выгоднее **32-битная** Pi OS, но карта
  записана 64-битной и переписывать её не стали: работать будет, а вся
  настройка платы у нас скриптами, так что решение обратимо за полчаса.
  Точка, где это может прижать, — scikit-learn на шагах 10-11: импорт
  scipy отъедает заметно. Если упрёмся в память — перепрошить на 32
- 12.09.2026 — плата поднялась: `deskpi`, пользователь `alex`, 192.168.1.205,
  карта 16 ГБ. Памяти в системе видно 415 МБ, в покое занято 143 — то есть
  64-битная Lite оставляет около 270 МБ. Приложению хватит, но запас на
  scikit-learn небольшой; см. пункт про 32 бита выше
- 12.09.2026 — система оказалась Debian **Trixie**, а скрипты писались под
  Bookworm. Что из этого вылезло на первом же прогоне:
  - paho-mqtt на плате **2.1.0**, конструктор клиента там требует версию
    API обратных вызовов, наш код звал его по-старому. Починено и проверено
    вживую: `MqttSource` подключается к брокеру на плате
  - mosquitto 2.x считает **повторное** объявление `persistence_location`
    фатальной ошибкой, а пакет Debian объявляет его сам в основном конфиге.
    Наш блок дублировал — брокер не стартовал, и `setup-step1.sh` умирал
    на середине. Теперь дописываем только недостающее
  - в `setup-step1.sh` был свой баг, к системе не относящийся: у `sed`
    разделителем стоял `|`, и он же использовался внутри выражения
- 12.09.2026 — **`sudo` на плате без пароля** (`/etc/sudoers.d/010-alex-nopasswd`),
  по просьбе Alex: плата стоит дома и наружу не смотрит. Из-за этого же
  решения не будет и пароля на брокере — пункт шага 6 остаётся в списке,
  но закрывать его Alex не собирается
- 12.09.2026 — **шаг 1 закрыт целиком.** Плата настроена, база создана,
  брокер работает, все интерфейсы ядра на месте
- 12.09.2026 — **гребёнка припаяна.** Стоит штырьками **вниз**, планка снизу
  платы — против обычая, но электрически всё равно. Отсюда постоянный риск
  зеркала: наша `распиновка.png` это вид сверху, и считать ножки надо,
  держа плату как на ней (microSD сверху, разъёмы слева, гребёнка справа,
  левый столбец нечётный). Переворачивать плату ради удобства нельзя
- 13.09.2026 — **экран заработал.** Питание оставили на **3,3 В (pin 17)**,
  а не на 5: какое напряжение нужно модулю, снаружи не определить, зато
  `get_throttled` остаётся `0x0` даже с включённой подсветкой — значит шина
  тянет, и пять вольт не нужны. Модуль — красная плата `480x320 v1.1`,
  тач XPT2046 на ней же
- 13.09.2026 — драйвер ST7796S пошёл **без единой правки**: и MADCTL, и
  `rotation_bgr = true`, и 32 МГц оказались верны с первого раза. Ожидали
  обратного, так что строчку «ждёт сверки на живой плате» можно снимать
- 13.09.2026 — **подсветка гаснет при выходе процесса**, и это не отказ:
  gpiozero освобождает GPIO18, ножка уходит во вход. Разовый скрипт,
  показавший кадр и завершившийся, оставляет чёрный экран. Проверять надо
  тем, что живёт: `bringup display` держит кадр вопросом, сервис — циклом.
  Зажечь вручную: `pinctrl set 18 op dh`
- 12.09.2026 — замыкания искали программно, мультиметра нет:
  `tools/find_bridges.py` подтягивает каждую свободную ножку вверх и вниз,
  затем подаёт единицу на одну и читает остальные. 17 ножек, 0 посаженных
  на землю, 0 на питание, 0 спаянных пар. Ножки под шинами читаются чисто,
  кроме GPIO7 и GPIO8 — их держит драйвер SPI. Под нагрузкой на 4 ядра
  45 секунд: 59 °C, `throttled=0x0`, частота не просела. Пайка прошла даром

## Расхождения с планом

| В плане | На деле |
|---|---|
| Экран ILI9341 2.4–2.8", 240×320, без тача | ST7796S 4", 480×320, **с резистивным тачем** |
| Всё на Python | Pi — Python, ПК-агент — C# (LibreHardwareMonitor это .NET-библиотека) |
| ML в ПК-агенте | ML на Pi: данные там же, а в ML.NET нет IsolationForest |
| Конфиг `config.yaml` | `config.toml` — `tomllib` в стандартной библиотеке, зависимостей ноль |
| Про аутентификацию брокера не сказано | Добавить пароль перед шагом 6 |
| Дашборд на шаге 14 | Скелет уехал на шаг 2: превью экранов окупается сразу |
